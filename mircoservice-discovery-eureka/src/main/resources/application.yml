server:
  port: 8761
spring:
  main:
    #允许实列覆盖
    allow-bean-definition-overriding: true

security:
  basic:
    enabled: true
  user:
    name: user
    password: password123
  #  role: admin

eureka:
  client:
    healthcheck:
      enbled: true
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://@localhost:8761/eureka

#mybatis:
  #mybatis 扫描mapper 文件的地址
 # mapper-locations: classpath:mapper/*.xml
  #配置mybatis 映射的实体包
  #type-aliases-package: com.skx.cloud.mircoservicediscoveryeureka.entity


